# 🎨 CollabBoard Frontend

This is the frontend component of the **CollabBoard** application, built with React and Socket.io.

## 🚀 Getting Started

1. **Install Dependencies**:
   ```bash
   npm install
   ```

2. **Setup Environment Variables**:
   Create a `.env` file in this directory:
   ```env
   REACT_APP_BACKEND_URL=http://localhost:5000
   REACT_APP_GOOGLE_CLIENT_ID=your_google_client_id
   ```

3. **Run the App**:
   ```bash
   npm start
   ```

## 🛠️ Tech Stack
- **React** for UI
- **Rough.js** for canvas drawing
- **Socket.io-client** for real-time synchronization
- **Tailwind CSS** for styling

For full project documentation, please refer to the [Root README](../README.md).
